from enum import Enum

class FilterKeys(Enum):
    AD_TYPE = "ad_type"
    REAL_ESTATE_TYPE = "real_estate_type"
    REGION = "region"
    METRO = "metro"
    FLOOR = "floor"
    AREA = "area"
    LAND_AREA = "land_area"
